import React from 'react';
import { style } from 'typestyle';

class ShouYeView extends React.Component {
    render() {
        return <div>
            ShouYeView
        </div>
    }
}

const sty = {
    wrap: style({
        width: '100%',
        height: '100vh',
        overflow: 'auto',
        display: 'flex'
    }),
}

export default ShouYeView
